import { System } from "@lastolivegames/becsy";

export const inputGroup = System.group();
export const logicGroup = System.group();
export const renderGroup = System.group();
export const cleanupGroup = System.group();
export const spawnerGroup = System.group();
